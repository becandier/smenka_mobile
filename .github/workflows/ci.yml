name: CI

# Обратная связь на ветках и PR. Сами проверки живут в переиспользуемом
# checks.yml — тот же workflow гейтит деплой в release-web.yml.

on:
  pull_request:
  push:
    branches-ignore: [main]

permissions:
  contents: read

jobs:
  checks:
    uses: ./.github/workflows/checks.yml
