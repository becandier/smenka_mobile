/// Тип чек-листа смены: на старте или на завершении.
///
/// Используется экземплярами чек-листов смены (`ChecklistInstance`) и
/// эффективными шаблонами участника (`EffectiveChecklistTemplate`).
/// Управление самими шаблонами вынесено в веб-админку.
enum ChecklistType { shiftStart, shiftEnd }
